import { createContext } from "react";

export const FileListContext=createContext<any>(undefined);
export const ActiveTeamContext=createContext<any>(undefined);
export const FileDataContext=createContext<any>(undefined);